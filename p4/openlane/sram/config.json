{
  "DESIGN_NAME": "sonic_sram_bank",
  "VERILOG_FILES": [
    "dir::../../../p2/rtl/sonic_sram_bank.sv"
  ],
  "VERILOG_INCLUDE_DIRS": [
    "dir::../../../p2/rtl"
  ],
  "SYNTH_PARAMETERS": [
    "ADDR_WIDTH=8"
  ],
  "// sram": "ADDR_WIDTH=8 (256 words) rather than the shipping 17 (128K words / 512 KB): every array in this design is flops today (T3.3/P2-12, not yet hard macros), so a full-depth bank is ~4M flops and not tractable through ABC. This is a flow-qualification run at reduced depth, same as LANES=4 for the router and TILE=8 for the tile -- see p4/RESULTS.md and HANDOFF.md T3.3 for the real fix (OpenRAM hard macros via LibreLane's MACROS key), which this run does not attempt.",
  "CLOCK_PORT": "clk",
  "CLOCK_PERIOD": 1000.0,
  "FP_SIZING": "absolute",
  "DIE_AREA": "0 0 1000 1000",
  "CORE_AREA": "10 10 990 990",
  "PL_TARGET_DENSITY": 0.45,
  "// die": "700x700/0.72 (2nd attempt, fixing the original 600x650/108% GPL-0301 failure) got past global placement but failed DETAILED placement post-CTS (DPL-0036), preceded by DPL-0035 hold/fanout buffer-count warnings hitting the 1000-buffer limit: ~80% initial utilization left no room for the CTS/hold resizer's inserted buffers to legalize into. This is the same class of problem as the router's own '40% utilisation leaves the router room' comment in config.json -- initial placement density has to leave real headroom for what CTS and the resizer add afterward, not just fit the synthesized cell count. 1000x1000 core at PL_TARGET_DENSITY=0.45 mirrors the router's margin (its working config uses FP_CORE_UTIL=40/PL_TARGET_DENSITY_PCT=45) rather than re-guessing a tighter number a third time.",
  "DESIGN_IS_CORE": 0,
  "// keys": "LibreLane errors on unrecognised config keys. Only keys beginning with // are exempt."
}
